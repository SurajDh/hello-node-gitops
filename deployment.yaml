apiVersion: apps/v1
kind: Deployment

metadata:
  name: hello-node

spec:
  replicas: 5

  selector:
    matchLabels:
      app: hello-node

  template:
    metadata:
      labels:
        app: hello-node

    spec:
      containers:
        - name: hello-node
          image: surajsdm/hello-node:54
          imagePullPolicy: Always

          ports:
            - containerPort: 3000

          env:
            - name: APP_VERSION
              value: "54"

            - name: BUILD_NUMBER
              value: "54"

            - name: GIT_COMMIT
              value: "a14512f"
              
